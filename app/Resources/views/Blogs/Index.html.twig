{% extends 'base.html.twig' %}

{% block body %}
</br></br></br></br>
   <table class='table table-bordered'>
    <thead>
      <tr>
        <th>#</th>
        <th>Title</th>
         <th>Created Date</th>
        <th>Descirption</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
    {% for blog in blogs %}
      <tr>
        <td scope=row>{{blog.id}}</td>
        <td>{{blog.title}}</td>
        <td>{{blog.description}}</td>
        <td>{{blog.createDate | date('F j, Y, g:i a')}}</td>
        <td>
        <a href=blogs/details/{{blog.id}} class='btn btn-success'>View</a>
        <a href=blogs/edit/{{blog.id}} class='btn btn-default'>Edit</a>
        <a href=blogs/delete/{{blog.id}} class='btn btn-danger'>Delete</a>
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
{% endblock %}